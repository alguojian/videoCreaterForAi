# Findings

- `VideoAspect.landscape` 已对应 1920×1080，素材下载层已有比例过滤。
- `app/services/video.py` 目前只渲染静态 SRT 字幕，适合在这里叠加独立重点词文字轨道。
- 现有主字幕采用白字、黑色描边、透明背景；重点词需要与它分层而不是替换它。
- 最终音频在 `generate_video()` 统一组装，可将短音效按重点词开始时间混入。
- `VideoParams` 是任务与渲染的统一参数入口；重点词开关、随机配色、动效和音效选项可在此扩展并由 WebUI 写入。
- WebUI 已有字幕参数面板；重点词设置应作为独立面板，避免和主字幕颜色、位置混在一起。
- `generate_final_videos()` 已将完整 `params` 传给 `video.generate_video()`，无需改变任务队列接口即可加入重点词图层。
- 音效改为随项目分发的固定素材库，而不是程序或模型实时生成；需要在实现阶段仅引入可商用授权的本地文件。
- 已知限制：本地浏览器预览会导致 Codex 应用重启，因此后续只通过生成帧和成片检查视觉效果。
- 2026-07-15 用户截图确认：重点词在右侧入场时可能超出画布；当前 `create_emphasis_text_clips()` 的 `slide_right` 直接把文字向右偏移 `0.8 * clip_width`，没有横向边界约束。
- 2026-07-15 音量测量：固定音效的原始平均电平约为 -18.7 至 -30.1 dB；现有 `0.16` 倍率再降低约 15.9 dB，导致最大峰值通常只有约 -16 至 -18 dB，容易被旁白掩盖。当前实现未做音效响度归一化。
- 2026-07-15 修复：重点词的静态、滑入、放大和摇晃帧均以 40px 横向安全边距定位；放大动画使用其实际帧宽度而非 MoviePy 动态 `clip.w` 的初始帧宽度，摇晃动画额外预留其 18px 位移。固定音效先归一到 0.85 峰值，再按默认 0.35 音量混入。
- 2026-07-15 成片验证：最终 `final-1.mp4` 的右侧放大重点词完整显示；与原旁白相减得到的最终 AAC 提示音残余峰值为 -16.5 dB，较旧样片约 -22 dB 的峰值提升约 6–7 dB。
- 2026-07-15 新截图诊断：重点词当前直接使用底部字幕的 `params.font_name`，因此没有独立标题字形；样式枚举仅有 `left/right`，渲染器也给每个重点词同一 `base_y`，没有同屏多词分层机制。
- 当前 Windows 原生字体包含 `HYZhongHeiTi-197.ttf`（汉仪中黑体）；项目资源包含 `MicrosoftYaHeiBold.ttc`，可作为专用标题字体不可用时的稳定兜底。
- 字体实测修正：`HYZhongHeiTi-197.ttf` 在此机器不含所需简体中文字形，重点词会显示方框，不能作为候选；`MicrosoftYaHeiBold.ttc` 可正常显示，后续还需和 Windows 原生 `simhei.ttf` 对比。
- 2026-07-15 音量复验：用户确认提示音实际存在，只是偏小；将默认混音倍率由 `0.35` 提至允许的上限 `0.50`，重新渲染后在 23.466 秒重点词窗口中，相对原旁白的残余峰值为 -13.1 dB，比此前 -16.5 dB 提高 3.4 dB。
- 2026-07-15 文字底部裁切根因：MoviePy 的 `TextClip(method="label")` 将中文文字和描边压到透明蒙版最底行；实际 `STHeitiMedium.ttc` 的 92px 重点词蒙版可见行范围是 4–91，而画布高度正好是 92。成片合成时该最底行会被截掉。
- 初次处理修正：样片实际使用 `MicrosoftYaHeiBold.ttc`，而初始回归测试误用了 `STHeitiMedium.ttc`。对微软雅黑，MoviePy 会把 `TextClip` 的 8px 垂直留白挤到顶部，底部仍为 0px；因此该处理未解决用户截图中的裁切。
- 最终处理：重点词不再用 MoviePy `TextClip`，改为 Pillow 根据 `textbbox` 创建带真实四边留白的 RGBA 图片，再交给 `ImageClip` 做动效。对样片字体测试确认底部保留 8px 透明空间。
- 2026-07-15 专用字体来源：将当前 Windows 原生 `C:\Windows\Fonts\simhei.ttf` 复制到项目 `resource/fonts/SimHei.ttf`，Pillow 实测可覆盖样片中文；这避免运行时依赖系统字体，但公开再分发前仍需确认字体授权范围。
- 多词同屏规则已经落地：同一字幕内按词在原文中的顺序计算开始时间，结束时间统一延长到该条字幕末尾；前三词依次占高、中、低层并优先分配不同的左、中、右位置，第四词开始轮换同层。
- 渲染器对重点词单独解析 `emphasis_font_name`；若指定字体不存在或缺少样片字形，回退到项目内 `MicrosoftYaHeiBold.ttc`。主字幕字体不受该设置影响。
- 实施过程记录：Task 1 第一次补丁因函数上下文不匹配未应用，重新读取准确位置后完成；Task 2 新测试第一次因漏导入 `EmphasisCue` 报 `NameError`，补齐导入后才得到真实 RED；Task 4 的首次多语言测试按预期显示 9 个语言包均缺少 `Emphasis Font`。
- 新横版样片抽帧确认：`婚姻二字` 与 `只有两笔` 在 1.84–2.80 秒同屏，`平凡日子` 与 `好好说话` 在 16.98–17.92 秒同屏；SimHei 字形与黑色描边四周均保留透明空间，没有底部裁切。
- 2026-07-19 参考视频复核：两条 1920×1080、30fps 视频都采用底部白字细黑描边主字幕与独立重点词叠层；重点词并非每句出现，水平位置以画面中心为主、垂直位置在上中部安全区变化。
- 2026-07-19 参考视频橙色像素统计的亮色中位数约为 RGB(254, 134, 27)；但用户明确要求重点词保留当前随机颜色策略，不将橙色设为唯一颜色。
- 2026-07-19 根因：emphasis._style_for() 先均匀选择 left/center/right，而 emphasis._arrange_grouped_cues() 又用洗牌后的三档位置覆盖它；因此不能表达中心优先的权重。
- 2026-07-19 根因：[local_voice] subtitle_max_chars=14 真实传入 cue builder，subtitle_max_lines=1 不被 LocalVoiceSettings 读取；将本地分段上限改为 18 是实现单行短字幕的有效入口。
- 2026-07-19 本机存在 C:\Windows\Fonts\simkai.ttf，项目字体目录没有该文件；重点词字体解析需要支持该系统路径并保持项目内 MicrosoftYaHeiBold.ttc 回退。
- 2026-07-19 关键词纵向层由 0.28/0.43/0.57 调整为 0.24/0.39/0.53，1920x1080 输出时约整体上移 43px，仍保留与底部主字幕的安全距离。
- 2026-07-19 当前关键词字体继续使用兼容路径 `resource/fonts/FZKaTongJianTi.ttf`，但文件内容已按用户后续要求替换为 `mengquti Regular`；当前 SHA-256 为 `BD3A83ACCC8E75FFD2159D7B801E2B4D71360AD6E78C3FD9E9F8EF843E30BAE9`，字形验证可覆盖样片中文。
- 2026-07-19 默认配音模式缺失时原本已回退自动配音，但默认音色仍为空；同时 WebUI 新会话、任务恢复和示例配置分别保留了匹配顺序关闭或 3 秒的旧回退，需一并统一。
- 2026-07-19 根因：普通字幕和 Markdown 重点词均直接使用整条字幕或整行的 `end`，因此一行含多个以句末标点分隔的句子时，前句重点词会延续到后句。用可见字符比例定位该词后首个 `。！？!?；;` 可在不改变对齐数据结构的前提下得到正确句末时间。
- 2026-07-20 参考视频像素复核：示例橙色字面中位数约为 RGB(255, 112, 0)，几乎是无明暗变化的纯色；放大原始 1920×1080 帧后确认视觉上的“黑边”主要来自右下投影，而不是原位纯黑硬描边。
- 2026-07-20 根因：旧实现把 17/13/9/5/1px 五层膨胀蒙版取最大值后整体偏移 5×9px，且最内层透明度为 255；这些轮廓在字形下方重叠，形成厚重的黑色台阶而非柔影。
- 2026-07-20 二次测量：参考帧右侧阴影拟合约为 17.5px 偏移、9px 高斯半径、90% 不透明度；底部拟合约为 12px 偏移、8px 半径、75% 不透明度。生产参数取中间稳定值：右移 14px、下移 10px、9px 高斯模糊、透明度 230/255，并将透明画布边距扩大到 52×48px，避免柔影尾部被裁掉。
- 2026-07-20 修正：关键词取消原位硬描边和字面明暗渐变，随机颜色直接作为纯色字面；字号按参考视频高度由默认 136 提升到 164。阴影只绘制一张黑色字形蒙版并进行一次高斯模糊，不再做 MaxFilter 膨胀或多层叠加。
