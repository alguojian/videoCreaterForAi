# Progress

## 2026-07-14

- 用户确认横版视频方向，并要求重点词可随机使用多种颜色和多种进场动效，同时带提示音。
- 用户确认重点词采用自动提取并允许手动修改。
- 用户确认每个重点词出现时均播放提示音。
- 用户要求音效采用固定精选库随机选择，拒绝每次模型生成导致的音质不稳定。
- 已将设计说明提交为 `2779652 docs: specify landscape emphasis effects`；尚未改动生产实现，等待用户审阅设计后进入实现。
- 已完成实施计划，准备按 TDD 顺序实现默认横版、重点词时间轴、固定 CC0 音效库、渲染混音和 WebUI 设置。
- 实施计划已通过范围、占位符和类型一致性自检；设计说明同步提交为 `90e0a33 docs: align emphasis animation rules`。
- 计划执行复核发现音效清单测试不能只信任 JSON 声明，已改为读取实际 WAV 时长；首次补丁因计划文本上下文不匹配失败一次，随后按当前内容重新应用。
- Task 1 完成：`7f29f89 feat: default video tasks to landscape`。`test_schema.py` 与 `test_cli.py` 共 34 项通过；新任务与 CLI 默认 16:9，重点词参数已加入 `VideoParams`。
- 本轮首次更新计划时，阶段标题与当前文件不一致导致补丁未匹配；已读取当前文本并按实际标题重新应用，未修改任何源码。
- Task 2 完成：先新增 LLM 重点词 JSON 解析与原文包含校验测试，确认缺少函数时按预期 RED；随后实现 `app.services.emphasis` 的不可变 cue、SRT 时间映射、稳定随机样式和 `generate_emphasis_terms()`。`test_emphasis.py` 与 `test_llm.py` 共 78 项通过、1 项跳过（16 个既有弃用警告）。
- Task 3 导入测试已通过（实际 WAV 时长由 Python `wave` 读取）；额外逐个 `ffprobe` 展示命令首次把变量赋值误写成调用，导致 PowerShell 报命令无效，音效导入本身未受影响，下一步将按正确赋值语法复核。
- Task 3 完成：新增可复现 PowerShell 导入脚本，从 Kenney UI Audio 的 CC0-1.0 源导入 12 个音效，统一为单声道 44.1kHz WAV；实际 `wave` 测试通过，`ffprobe` 逐一确认最长 `sparkle-02.wav` 为 0.419410 秒。
- Task 4 完成：任务入口测试先确认 `emphasis.json` 尚未写入时按预期失败；随后在字幕阶段之后接入 cue 生成，最终视频调用和任务结果均携带 `emphasis_path`。`test_task.py -k emphasis` 通过（1 项通过、11 个既有弃用警告）。
- Task 5 完成：先验证缺少文字/音效 helper 和最终渲染参数时按预期 RED；随后实现六种重点词进场动效、字幕上方安全区叠加和本地提示音混音。`test_video.py -k emphasis` 3 项通过，`compileall` 通过（11 个既有弃用警告）。
- Task 6 首次 WebUI 补丁因函数边界锚点过宽未匹配，未写入 `Main.py`；已记录后将按字幕函数结尾和生成面板调用点分段应用。
- Task 6 完成：新增重点词开关、手动覆盖、随机配色/动效、固定提示音与音量控件，并恢复历史任务的这些状态；9 个语言包均加入所需翻译。语言包测试 1 项通过（9 个子测试），`Main.py` 编译和全部 locale JSON 解析通过。
- 样片首次运行在 CosyVoice 语音阶段异常终止，只留下 `generate.request.json`；系统日志同时记录了连续的 `nvlddmkm` 事件和 NVIDIA LocalSystem Container 崩溃。对同一请求设置 `CUDA_VISIBLE_DEVICES=-1` 后不再触发显卡异常，但 CosyVoice ONNX 前端报 `bad allocation` 并以退出码 1 失败。样片将复用既有成功任务的本地 WAV/SRT 来验收横版重点词渲染，不改动正式 TTS 路径。
- 样片进程检查发现两个 CosyVoice worker 在 GPU 异常后成为孤儿进程（PID 38420、32892），仍持续占用 CPU；四个样片父进程也在等待或重复合成。已按命令行确认它们均来自当前工作区和 `marriage-pixabay-landscape-test`，下一步将停止这些本轮孤儿进程，避免继续诱发显卡驱动错误。
- 清理已核验的样片孤儿进程后，显卡事件归零。实际横版渲染首次暴露 MoviePy 对超短 `AudioFileClip` 批量取样越界的问题；新增回归测试后改为将精选 PCM WAV 读为 `AudioArrayClip`，越界时间自动静音。`test_video.py -k emphasis` 3 项通过。
- 横版样片完成：`storage/tasks/marriage-pixabay-landscape-test/final-1.mp4`，1920×1080、H.264/AAC、25.13 秒、9,180,780 字节。截图验证底部白字黑描边无背景，重点词随机颜色和位置均已叠加；未使用浏览器预览页面。
- 最终全量验证：`python -m pytest -q --ignore=third_party` 完成，399 项通过、7 项跳过、98 个子测试通过；仅有现有 Pydantic、FastAPI、Starlette 与 pydub 弃用警告。

## 2026-07-15

- 用户反馈横版样片中重点词可能显示不全，且重点词出现时听不到提示音；已从截图确认并开始按渲染边界、音效素材和最终混音分层排查。
- 已定位文字越界路径：右侧入场动画没有对 `x` 坐标进行画布安全边界裁剪。音量测量显示短音效以 `0.16` 原始倍率混入，未归一化，峰值容易低于旁白。
- 诊断时第一次读取样片任务根目录 `generate.request.json` 失败；该文件实际位于 `local_voice/block-001/`，后续已改用重点词清单和成片音频测量继续排查，未重复相同路径。
- 已完成 TDD 修复：新增默认音量、滑入安全区、音效峰值归一化和放大帧安全区测试。首次放大帧修复失败后，确认 MoviePy 动态 `clip.w` 表示初始缩放帧而非最大渲染帧；改为在缩放前记录基准宽高并以最大动画尺寸定位，相关定向测试 8 项通过。
- 已重渲染横版样片（仅复用已有旁白、字幕和素材，不启动 CosyVoice）。成片为 H.264/AAC、1920×1080、25.13 秒、9,171,852 字节；右侧放大重点词抽帧完整显示。最终提示音残余峰值 -16.5 dB，较旧样片约提升 6–7 dB。
- 最终全量验证：`python -m pytest -q --ignore=third_party` 完成，403 项通过、7 项跳过、98 个子测试通过；仅有既有弃用警告。
- 提交前额外加入窄竖版左侧长重点词的摇晃边界测试：第一次用例未到边界而错误通过，已改为真实触发场景并确认原实现失败；预留 18px 摇晃位移后，定向测试 9 项通过，全量测试更新为 403 项通过。
- 用户反馈新版重点词仍不符合参考样式：需要独立标题字体、支持同屏多个词、第一词更高、横向位置限定左中右且动画随机。已开始设计阶段，未改动生产代码。
- 字体诊断中首次生成静态对比图时，行坐标变量作用域错误；已改为按固定索引生成。对比证实本机 `HYZhongHeiTi-197.ttf` 缺少简体中文字形，已排除该字体，未改动生产代码。
- 额外枚举固定音效素材的 PowerShell 诊断命令存在括号配对错误，报 `ParserError: Unexpected token '}'`；用户随即确认音效并非缺失而是偏小，因此没有用该失败命令作为结论。
- 用户确认提示音确实存在但声音偏小。已先更新回归测试使默认重点词音量预期为 `0.50`（原默认实现 `0.35` 时按预期失败），再同步调整任务模型、WebUI 初始值和样片脚本。
- 已复用原有旁白、字幕和横版素材重新编码样片：`final-1.mp4` 为 H.264/AAC、1920×1080、30fps、25.13 秒、9,171,400 字节。23.466 秒重点词窗口的音效残余峰值为 -13.1 dB，较上一版 -16.5 dB 提高 3.4 dB。
- 本次完整验证：`python -m pytest -q --ignore=third_party` 通过，403 项通过、7 项跳过、98 个子测试通过；仅有既有弃用警告。
- 用户进一步澄清“显示不全”是重点词字形底部被裁切，而非文案漏字。检查 `TextClip` 蒙版确认可见像素压到画布末行；新增垂直留白测试先按预期失败（第 91 行不小于画布最后一行 91）。
- 已为重点词文字画布增加 8px 透明留白，并重新生成横版样片。测试先前失败、修复后 8 项重点词定向测试通过；最终抽帧确认字形底部和黑描边完整。全量测试更新为 404 项通过、7 项跳过、98 个子测试通过。
- 用户再次截图确认上述样片仍有底部裁切。进一步对照发现该样片用 `MicrosoftYaHeiBold.ttc`，但垂直留白测试当时用了 `STHeitiMedium.ttc`；MoviePy 对微软雅黑的 `TextClip` 实际把可见蒙版压到画布第 111 行末行，复现测试按预期失败。
- 已改用 Pillow 的 `textbbox` 和 RGBA 透明图片来渲染重点词，再由 MoviePy `ImageClip` 负责位置和动效。实际样片字体的回归测试通过，成片已重新生成（1920×1080、30fps、25.13 秒、9,194,707 字节）；为避免播放器缓存旧 `final-1.mp4`，额外复制为 `final-pillow-text-1.mp4`。全量测试保持 404 项通过、7 项跳过、98 个子测试通过。
- 用户确认采用方案 A，并要求先用 SimHei 测试且把字体放入项目。已提交设计和实施计划，明确同字幕多词依次入场并保持、三层布局、左中右稳定随机、超过三词轮换。
- Task 1 完成：新增 `emphasis_font_name` 参数并将 Windows 原生 SimHei 复制进项目，提交 `d6cf6ca feat: package dedicated SimHei emphasis font`；schema 定向测试 5 项通过。
- Task 2 完成：重点词清单加入 `subtitle_index` 与 `layer`，同字幕多词共享结束时间并按三层/三位置排列，旧 JSON 仍可读取；提交 `264dfd2 feat: layer simultaneous emphasis terms`，定向测试 9 项通过。
- Task 3 完成：重点词字体与主字幕字体解耦，渲染坐标使用高、中、低三层和左、中、右三点；提交 `e36fceb feat: render emphasis terms in three slots`，重点词视频测试 10 项通过。
- Task 4 完成：WebUI 增加重点词字体选择、历史任务恢复并补齐 9 个语言包；提交 `33edbd4 feat: configure emphasis font in webui`，WebUI/i18n 测试 15 项通过（9 个子测试）。
- Task 5 样片完成：复用已有口播音频、SRT 和 6 段横版素材重新编码，没有联网下载或调用 TTS。新文件 `final-simhei-multi-term-1.mp4` 为 H.264/AAC、1920×1080、30fps、25.13 秒、9,310,835 字节。
- 两处同屏抽帧已人工检查：第一词位于更高层、第二词位于中层，横向位置来自左中右三档；SimHei 字形底部与描边完整。最终全量验证为 410 项通过、7 项跳过、98 个子测试通过，仅有既有弃用警告。
