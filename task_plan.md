# 横版重点词动效与音效

## 目标

将视频默认输出切换为横版 16:9，并在保留底部主字幕的基础上，增加可自动生成、可配置颜色与动效的重点词二次提醒，以及与重点词时间一致的提示音。

## 阶段

- [x] 阶段 1：检查当前横版、字幕和音频合成能力
- [x] 阶段 2：确认重点词生成与音效的产品规则
- [x] 阶段 3：形成设计说明，等待用户确认
- [x] 阶段 4：测试驱动实现重点词时间轴、动效和混音
- [x] 阶段 5：生成横版样片并验收
- [x] 阶段 6：修复重点词越界与音效可感知性
- [x] 阶段 7：重点词专用字体与多词同屏布局

## 当前决策

- 横版输出使用已有 `16:9` 枚举和素材比例过滤。
- 底部白色单行字幕继续保留，重点词是单独的上层文字轨道。
- 重点词默认自动提取，用户可用 WebUI 手动覆盖自动结果。
- 每个重点词都播放一次短提示音，音量需低于人声且不覆盖旁白。
- 音效使用本地固定精选素材库（约 12 个短音效），不使用生成模型；只在该库中随机选择。
- 同一任务采用稳定随机配色、动效和音效，保证重新生成时效果可复现；新任务会产生新的组合。
- 不再使用本地浏览器对比页；视觉验收通过实际视频截图完成。
- 重点词在入场和静止两个阶段都必须完整落在画布安全区内；音效必须先归一化，再按可感知的前景目标音量混音。
- 重点词使用独立标题字体；同屏多个重点词按出现顺序自上而下分层，横向位置只在左、中、右三档中稳定随机，动画独立随机。
- 重点词默认使用项目内 `resource/fonts/SimHei.ttf`，不再依赖目标机器是否安装黑体。
- 同一字幕中的重点词按文案出现顺序入场并保持到该字幕结束；前三个占用高、中、低三层，超过三个时从高层开始轮换。
