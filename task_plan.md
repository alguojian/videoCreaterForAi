# 横版重点词动效与音效

## 目标

将视频默认输出切换为横版 16:9，并在保留底部主字幕的基础上，增加可自动生成、可配置颜色与动效的重点词二次提醒，以及与重点词时间一致的提示音。

## 阶段

- [x] 阶段 1：检查当前横版、字幕和音频合成能力
- [x] 阶段 2：确认重点词生成与音效的产品规则
- [x] 阶段 3：形成设计说明，等待用户确认
- [x] 阶段 4：测试驱动实现重点词时间轴、动效和混音
- [x] 阶段 5：生成横版样片并验收
- [x] 阶段 6：修复重点词越界与音效可感知性
- [x] 阶段 7：重点词专用字体与多词同屏布局
- [x] 阶段 8：参考样式字幕与加权重点词
- [x] 阶段 9：底部字幕默认与思源黑体真实加粗
- [x] 阶段 10：关键词整体上移与方正卡通字体接入预留
- [x] 阶段 11：打包项目内关键词专用字体
- [x] 阶段 12：新任务默认配音、素材顺序和片段时长
- [x] 阶段 13：重点词按所属句末消失
- [x] 阶段 14：重点词参考视频定向投影与柔影重构

## 当前决策

- 横版输出使用已有 `16:9` 枚举和素材比例过滤。
- 底部白色单行字幕继续保留，重点词是单独的上层文字轨道。
- 重点词默认自动提取，用户可用 WebUI 手动覆盖自动结果。
- 每个重点词都播放一次短提示音，音量需低于人声且不覆盖旁白。
- 音效使用本地固定精选素材库（约 12 个短音效），不使用生成模型；只在该库中随机选择。
- 同一任务采用稳定随机配色、动效和音效，保证重新生成时效果可复现；新任务会产生新的组合。
- 不再使用本地浏览器对比页；视觉验收通过实际视频截图完成。
- 重点词在入场和静止两个阶段都必须完整落在画布安全区内；音效必须先归一化，再按可感知的前景目标音量混音。
- 重点词使用独立标题字体；同屏多个重点词按出现顺序自上而下分层，横向位置只在左、中、右三档中稳定随机，动画独立随机。
- 重点词默认使用项目内 `resource/fonts/SimHei.ttf`，不再依赖目标机器是否安装黑体。
- 同一字幕中的重点词按文案出现顺序入场并保持到该字幕结束；前三个占用高、中、低三层，超过三个时从高层开始轮换。
- 2026-07-19 用户确认参考样式：重点词颜色仍随机；横向位置按左 15%、中 70%、右 15% 加权随机；纵向继续用上中部安全层；动画统一轻弹出；音效文件、混音与音量控制保持当前本地实现。
- 2026-07-19 用户确认主字幕保持单行，本地字幕切分上限由 14 个字调整为 18 个字。
- 2026-07-19 用户确认重点词使用本机 SimKai 风格字体；缺失或缺字时回退项目内稳定字体。
- 2026-07-19 用户确认主字幕默认固定底部；重点词改用本机 `NotoSansSC-VF.ttf` 的真实 700 字重，并略微放大。
- 2026-07-19 用户确认方正卡通简体仅用于重点词；待提供已授权安装的字体文件后替换重点词字体选项，不影响主字幕。三层重点词整体上移约 46px。
- 2026-07-19 用户提供已下载的方正卡通简体字体；以 `resource/fonts/FZKaTongJianTi.ttf` 随项目分发，关键词字体固定为该文件，主字幕字体列表不变。
- 2026-07-19 用户随后提供新字体并要求替换旧字体；为兼容历史任务参数继续保留 `FZKaTongJianTi.ttf` 文件名，文件内容已更新为当前 `mengquti Regular`，未引用的重复字体副本不再保留。
- 2026-07-19 用户确认新任务默认自动配音并使用 `local:default`，单片段 5 秒，勾选按文案顺序匹配画面；历史任务恢复仍保留自己的参数。
- 2026-07-19 用户确认重点词只应展示到关联句子结束；同一 Markdown 行或字幕中出现多句时，前一句重点词不得停留到后一句。
- 2026-07-20 用户确认重点词外圈应为黑色透明度渐变，不接受向下堆叠的厚黑描边。按原始 1920×1080 参考视频复核后，实现固定为纯色字面、无硬描边、向右下偏移的单层高斯投影；颜色继续跟随每个重点词。
